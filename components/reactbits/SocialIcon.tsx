// Re-export shim: SocialIcon lives at @/components/SocialIcon. This alias makes the
// @/components/reactbits/SocialIcon path resolve too (generators sometimes guess it).
export { default, SocialIcon } from '../SocialIcon';
